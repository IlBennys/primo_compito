import React from "react"

const bottone = (prop) => {
  return (
    <button
      style={{
        color: prop.colore,
        backgroundColor: prop.coloreDue,
        marginBottom: "15px",
      }}
    >
      prova
    </button>
  )
}

export default bottone
